import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: '#236A62',
    },
    secondary: {
      main: '#E7B692',
    },
    background: {
      default: '#ddd',
      paper: '#fcf3ed',
    },
  },
})

export default theme