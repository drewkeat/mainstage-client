const initialState = {
  authorized: false,
  errors: null
};

const Auth = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default Auth